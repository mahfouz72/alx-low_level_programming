#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
  *main - Entry point
  *Discription: chech for number
  *Return: always 0
  */
int main(void)
{
	int n;

	for (n = 0; n < 9; n++)
	{
		printf("%d", n);
	}
	return (0);
}
